import { buildEmailSubscriberCollection } from "./content";

export const EmailSubscribers = buildEmailSubscriberCollection(
  "email-subscribers",
  "Email Subscriber",
  "Email Subscribers",
  "Newsletter and lead capture subscribers.",
);
